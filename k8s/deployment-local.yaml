apiVersion: apps/v1
kind: Deployment
metadata:
  name: setpoint-operator
  namespace: default
  labels:
    app: setpoint-operator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: setpoint-operator
  template:
    metadata:
      labels:
        app: setpoint-operator
    spec:
      serviceAccountName: setpoint-operator
      containers:
        - name: setpoint
          image: setpoint-operator:latest
          imagePullPolicy: Never
          ports:
            - name: metrics
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: metrics
            initialDelaySeconds: 5
            periodSeconds: 5
          env:
            - name: RUST_LOG
              value: info
            - name: HOSTNAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          resources:
            limits:
              cpu: 500m
              memory: 256Mi
            requests:
              cpu: 100m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: setpoint-operator-metrics
  namespace: default
  labels:
    app: setpoint-operator
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/port: "8080"
    prometheus.io/path: "/metrics"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    app: setpoint-operator
