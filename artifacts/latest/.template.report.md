# Setpoint Flagship Proof Report (TEMPLATE)

This is a **template** — it shows the shape of the generated report but contains no real proof data. A real proof run overwrites this file.

- Generated: 1970-01-01T00:00:00Z
- PLC: `line-1-printer-plc`
- Operator namespace: `default`

## Outcome

| Stage        | Phase        | Message |
|--------------|--------------|---------|
| Pre-drift    | Connected    | All registers in sync |
| Post-drift   | DriftDetected | Drift detected on register(s): print-head-position |

- Total drift events before injection: **0**
- Total drift events after injection:  **1**
- Total corrections applied:           **0** (Alert-policy register: no correction by design)

## Per-register state

### Before drift injection

  BEFORE | conveyor-speed @ 4001 | desired=2500 | current=2500 | in_sync=true  | drift_events=0 | corrections=0 | strategy=Auto
  BEFORE | print-head-position @ 4002 | desired=1200 | current=1200 | in_sync=true | drift_events=0 | corrections=0 | strategy=Alert

### After drift injection

  AFTER  | conveyor-speed @ 4001 | desired=2500 | current=2500 | in_sync=true  | drift_events=0 | corrections=0 | strategy=Auto
  AFTER  | print-head-position @ 4002 | desired=1200 | current=9999 | in_sync=false | drift_events=1 | corrections=0 | strategy=Alert

## What this proves (when real)

- **Detection works:** the operator detected drift on the `print-head-position` register within one poll interval of the simulator starting to write.
- **Per-policy remediation works:** the Alert-policy register was *not* auto-corrected, even though the operator saw the drift. The Auto-policy register was not drifted, so no auto-correction was needed.
- **No false positives:** the `conveyor-speed` register, which the simulator was not touching, stayed in sync throughout the run.
