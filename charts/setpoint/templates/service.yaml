apiVersion: v1
kind: Service
metadata:
  name: {{ include "setpoint.fullname" . }}-metrics
  labels:
    {{- include "setpoint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "setpoint.selectorLabels" . | nindent 4 }}
