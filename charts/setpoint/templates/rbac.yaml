{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "setpoint.serviceAccountName" . }}
  labels:
    {{- include "setpoint.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: {{ include "setpoint.fullname" . }}
  labels:
    {{- include "setpoint.labels" . | nindent 4 }}
rules:
  - apiGroups:
      - setpoint.io
    resources:
      - industrialplcs
    verbs:
      - get
      - list
      - watch
      - create
      - update
      - patch
      - delete
  - apiGroups:
      - setpoint.io
    resources:
      - industrialplcs/status
    verbs:
      - get
      - update
      - patch
  - apiGroups:
      - setpoint.io
    resources:
      - industrialplcs/finalizers
    verbs:
      - update
  - apiGroups:
      - ""
    resources:
      - events
    verbs:
      - create
      - patch
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ include "setpoint.fullname" . }}
  labels:
    {{- include "setpoint.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ include "setpoint.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "setpoint.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end }}
